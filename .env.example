# Backend
VITE_API_BASE=         # FastAPI URL — leave empty for mock data
VITE_TENANT_ID=        # dev-only fallback tenant_id

# Build / deploy
VITE_APP_VERSION=dev   # injected by CI
VITE_SITE_URL=http://localhost:3000
